"""Unit tests, mirroring the layout of ``src/``.

One test module per source module, split further only where a source module is
large enough that a single test file would be unreadable
(``source_data_loader.py`` and ``bb_excel_pipeline.py``).

Tests that span several modules are not unit tests and live in ``tests/route/``.
"""

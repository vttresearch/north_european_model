"""Test suite for north_european_model.

A package (rather than a bare folder) so that test modules can import shared
infrastructure as ``from tests._common.contracts import ...`` and so that two
test modules in different subfolders may share a basename.
"""
